<!DOCTYPE html>
<html lang="en">

<head>

    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Clothes Shop</title>
        <link rel="stylesheet" type="text/css" href="//cdn.jsdelivr.net/gh/kenwheeler/slick@1.8.1/slick/slick.css" />
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha2/dist/css/bootstrap.min.css" rel="stylesheet"
            integrity="sha384-aFq/bzH65dt+w6FI2ooMVUpc+21e0SRygnTpmBvdBgSdnuTN7QbdgL+OapgHtvPp" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.3.0/css/all.min.css"
            integrity="sha512-SzlrxWUlpfuzQ+pcUCosxcglQRNAq/DZjVsC0lE40xsADsfeQoEypE+enwcOiGjk/bSuGGKHEyjSoQ1zVisanQ=="
            crossorigin="anonymous" referrerpolicy="no-referrer" />
        <link rel="stylesheet" href="css/home.css">
        <link rel="stylesheet" href="/css/header.css">

    </head>

<body>
    {{> header}}
    <div class="content">
        <div class="container-fluid container-welcome" style="padding: 0px">
            <div class="item-carousel">
                <div class="welcome">
                    <img class="welcome-img" src="img/slider1.jpg" alt="">
                    <span class="welcome-content welcome1">Chào mừng đến với HShop</span>
                </div>
                <div class="welcome ">
                    <img class="welcome-img" src="img/slider2.jpg" alt="">
                    <span class="welcome-content welcome2">Chào mừng đến với HShop</span>
                </div>
                <div class="welcome ">
                    <img class="welcome-img" src="img/slider3.jpg" alt="">
                    <span class="welcome-content welcome3">Chào mừng đến với HShop</span>
                </div>
            </div>
        </div>
        <div class="container-fluid container-outstanding">
            <div class="headline">
                <h2>SẢN PHẨM NỔI BẬT</h2>
                <h3>Xem các sản phẩm nổi bật tại đây</h3>
            </div>

            <div class="product-carousel">
                {{#if outstandingProducts}}
                {{#each outstandingProducts}}
                <div class="product">
                    <a href="/product/{{this._id}}" style="text-decoration: none;">
                        <div class="product__image">
                            <img src="{{this.main_image}}" alt="">
                        </div>
                        <div class="product__name">{{this.name}}</div>
                        <div class="product__price">{{this.price}}<sup>đ</sup>
                            {{#if (isNotNull this.old_price)}}
                            <span>{{this.old_price}}<sup>đ</sup></span>
                            {{/if}}
                        </div>
                    </a>
                </div>
                {{/each}}
                {{else}}
                <p>Không tìm thấy sản phẩm</p>
                {{/if}}
            </div>
        </div>

        <div class="container-fluid container-new">
            <div class="headline">
                <h2>SẢN PHẨM MỚI NHẤT</h2>
                <h3>Xem các sản phẩm mới nhất tại đây</h3>
            </div>

            <div class="product-carousel">
                {{#if newProducts}}
                {{#each newProducts}}
                <div class="product">
                    <a href="/product/{{this._id}}" style="text-decoration: none;">
                        <div class="product__image">
                            <img src="{{this.main_image}}" alt="">
                        </div>
                        <div class="product__name">{{this.name}}</div>
                        <div class="product__price">{{this.price}}<sup>đ</sup>
                            {{#if (isNotNull this.old_price)}}
                            <span>{{this.old_price}}<sup>đ</sup></span>
                            {{/if}}
                        </div>
                    </a>
                </div>
                {{/each}}
                {{else}}
                <p>Không tìm thấy sản phẩm</p>
                {{/if}}
            </div>
        </div>
    </div>
    {{>footer}}
    <script type="text/javascript">
        $(document).ready(function () {
            $('.product-carousel').slick({
                slidesToShow: 4,
                mobileFirst: true,
                slidesToScroll: 1,
                autoplay: true,
                autoplaySpeed: 2000,
                adaptiveHeight: false,
                prevArrow: '<button type="button" class="slick-prev"><</button>',
                nextArrow: '<button type="button" class="slick-next">></button>',
                responsive: [
                    {
                        breakpoint: 1300,
                        settings: {
                            slidesToShow: 4
                        }
                    },
                    {
                        breakpoint: 800,
                        settings: {
                            slidesToShow: 3
                        }
                    },
                    {
                        breakpoint: 700,
                        settings: {
                            slidesToShow: 2
                        }
                    },
                    {
                        breakpoint: 400,
                        settings: {
                            slidesToShow: 1
                        }
                    }],

            });
        });

        $(document).ready(function () {
            $('.item-carousel').slick({
                slidesToShow: 1,
                slidesToScroll: 1,
                autoplay: true,
                autoplaySpeed: 4000,
                adaptiveHeight: false,
                prevArrow: '<button type="button" class="slick-prev"><</button>',
                nextArrow: '<button type="button" class="slick-next">></button>',
            });
        });

        function getSlidesToShow() {
            if ($(window).width() <= 767) {
                return 1;
            } else if ($(window).width() <= 991) {
                return 2;
            } else if ($(window).width() <= 1199) {
                return 3;
            } else {
                return 4;
            }
        }
    </script>
    <script type="text/javascript" src="//cdn.jsdelivr.net/gh/kenwheeler/slick@1.8.1/slick/slick.min.js"></script>

</body>
<footer></footer>


</html>